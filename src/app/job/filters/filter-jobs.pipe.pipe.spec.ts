import { Filters\SearchListPipePipe } from './filters\search-list-pipe.pipe';

describe('Filters\SearchListPipePipe', () => {
  it('create an instance', () => {
    const pipe = new Filters\SearchListPipePipe();
    expect(pipe).toBeTruthy();
  });
});
